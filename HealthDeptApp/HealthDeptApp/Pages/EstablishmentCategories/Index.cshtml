@page
@model HealthDeptApp.Pages.EstablishmentCategories.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EstablishmentCategory[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstablishmentCategory[0].InspectionFrequency)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EstablishmentCategory[0].CanHaveLiquorLicense)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.EstablishmentCategory) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InspectionFrequency)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CanHaveLiquorLicense)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
